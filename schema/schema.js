const graphql = require('graphql');
const axios =  require(axios);

const {
    GraphQLObjectType,
    GraphQLInt,
    GraphQLString,
    GraphQLBoolean,
    GraphQLList,
    GraphQLSchema,
    GraphQLNonNull,
    GraphQLID,
    ObjectID,
  } = graphql;